import app_action_config from "./rollup.app-action.config"
import app_options_config from "./rollup.app-options.config"
import app_sidebar_config from "./rollup.app-sidebar.config"
import background_config from "./rollup.background.config"

const config = [
  background_config,
  app_action_config,
  app_options_config,
  app_sidebar_config,
]

export default config
